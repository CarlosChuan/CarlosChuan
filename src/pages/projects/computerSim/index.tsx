import { Grid } from "../../../components/shared/Grid";

export const ComputerSimProject = () => {
	return (
		<Grid
			style={{
				display: "flex",
				flexDirection: "column",
				flexGrow: 1,
			}}
		></Grid>
	);
};
